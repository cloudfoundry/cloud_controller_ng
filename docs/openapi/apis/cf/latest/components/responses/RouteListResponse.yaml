description: A list of routes
content:
  application/json:
    schema:
      type: object
      properties:
        pagination:
          $ref: '../schemas/Pagination.yaml'
        resources:
          type: array
          items:
            $ref: '../schemas/Route.yaml'
