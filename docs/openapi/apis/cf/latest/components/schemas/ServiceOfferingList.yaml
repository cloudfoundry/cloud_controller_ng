type: object
description: A list of service offerings
properties:
  pagination:
    $ref: './Pagination.yaml'
  resources:
    type: array
    items:
      $ref: './ServiceOffering.yaml'
    description: List of service offerings
  included:
    $ref: './IncludedResources.yaml'
    description: Additional related resources included in the response when using the fields parameter
