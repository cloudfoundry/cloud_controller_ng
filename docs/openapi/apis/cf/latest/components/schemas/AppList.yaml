type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of apps
  resources:
    type: array
    items:
      $ref: './App.yaml'
    description: List of apps
  included:
    $ref: './IncludedResources.yaml'
    description: Additional related resources included in the response when using the include parameter
description: A paginated list of apps
