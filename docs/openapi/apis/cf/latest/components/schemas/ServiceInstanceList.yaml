type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of service instances
  resources:
    type: array
    items:
      oneOf:
        - $ref: './ManagedServiceInstance.yaml'
        - $ref: './UserProvidedServiceInstance.yaml'
      discriminator:
        propertyName: type
        mapping:
          managed: './ManagedServiceInstance.yaml'
          user-provided: './UserProvidedServiceInstance.yaml'
    description: List of service instances
description: A paginated list of service instances
