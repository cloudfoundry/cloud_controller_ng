type: object
allOf:
  - $ref: './Pagination.yaml'
  - properties:
      resources:
        type: array
        items:
          $ref: './OrganizationQuota.yaml'
description: A paginated list of organization quotas
