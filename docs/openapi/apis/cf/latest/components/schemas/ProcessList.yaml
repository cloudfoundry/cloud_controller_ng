type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of processes
  resources:
    type: array
    items:
      $ref: './Process.yaml'
    description: List of processes
description: 'A paginated list of processes'
