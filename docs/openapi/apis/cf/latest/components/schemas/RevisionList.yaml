type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of revisions
  resources:
    type: array
    items:
      $ref: './Revision.yaml'
    description: List of revisions
description: A paginated list of revisions
