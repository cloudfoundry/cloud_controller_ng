type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of app features
  resources:
    type: array
    items:
      $ref: './AppFeature.yaml'
    description: List of app features
description: A list of the features available for an app
