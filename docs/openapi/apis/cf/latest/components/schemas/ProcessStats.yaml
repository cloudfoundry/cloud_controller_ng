type: object
properties:
  resources:
    type: array
    items:
      $ref: './ProcessStat.yaml'
    description: The stats for each instance of the process
description: 'Process stats are objects that represent the individual instances of a process'
