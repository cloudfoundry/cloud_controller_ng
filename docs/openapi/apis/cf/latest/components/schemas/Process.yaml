type: object
allOf:
  - $ref: './BaseSchema.yaml'
  - properties:
      version:
        type: string
        format: uuid
        description: Random identifier that changes every time the process will be recreated in the runtime
      type:
        type: string
        description: Process type; a unique identifier for processes belonging to an app
      command:
        type: [string, "null"]
        description: The command used to start the process; use null to revert to the buildpack-detected or procfile-provided start command
      user:
        type: [string, "null"]
        description: The user used to run the process; use null to revert to the docker-detected or default 'vcap' user
      instances:
        type: integer
        description: The number of instances to run
      memory_in_mb:
        type: [integer, "null"]
        description: The memory in MB allocated per instance
      disk_in_mb:
        type: [integer, "null"]
        description: The disk in MB allocated per instance
      log_rate_limit_in_bytes_per_second:
        type: integer
        description: The log rate in bytes per second allocated per instance
      health_check:
        $ref: './HealthCheck.yaml'
        description: The health check to perform on the process
      readiness_health_check:
        $ref: './ReadinessHealthCheck.yaml'
        description: The readiness health check to perform on the process
      process_instances:
        type: array
        items:
          $ref: './ProcessInstance.yaml'
        description: The instances of the process; this key is only present when `process_instances` is requested via the `embed` query parameter (experimental)
      relationships:
        type: object
        description: Relationships to other resources
        properties:
          app:
            $ref: './RelationshipToOne.yaml'
            description: The app the process belongs to
          revision:
            oneOf:
              - $ref: './RelationshipToOne.yaml'
              - type: 'null'
            description: The app revision the process is currently running
      metadata:
        $ref: './Metadata.yaml'
        description: Labels and annotations applied to the process
      links:
        type: object
        description: Links to related resources
        properties:
          self:
            $ref: './Link.yaml'
            description: The URL to get this process
          scale:
            $ref: './Link.yaml'
            description: The URL to scale this process
          app:
            $ref: './Link.yaml'
            description: The URL to get the app for this process
          space:
            $ref: './Link.yaml'
            description: The URL to get the space for this process
          stats:
            $ref: './Link.yaml'
            description: The URL to get the stats for this process
          process_instances:
            $ref: './Link.yaml'
            description: The URL to get the instances for this process
description: 'A process defines the runnable units of an app'
