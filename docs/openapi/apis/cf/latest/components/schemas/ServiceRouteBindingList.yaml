type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of service route bindings
  resources:
    type: array
    items:
      $ref: './ServiceRouteBinding.yaml'
    description: List of service route bindings
  included:
    $ref: './IncludedResources.yaml'
    description: Additional related resources included in the response when using the `include` parameter
description: A paginated list of service route bindings
