type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
  resources:
    type: array
    description: List of space quotas
    items:
      $ref: './SpaceQuota.yaml'
description: A paginated list of space quotas
