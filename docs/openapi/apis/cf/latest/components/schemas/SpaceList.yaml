type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of spaces
  resources:
    type: array
    items:
      $ref: './Space.yaml'
    description: List of spaces
  included:
    $ref: './IncludedResources.yaml'
    description: Additional related resources included in the response when using the include parameter
