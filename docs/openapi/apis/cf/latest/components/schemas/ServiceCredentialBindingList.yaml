type: object
properties:
  pagination:
    $ref: './Pagination.yaml'
    description: Pagination information for the list of service credential bindings
  resources:
    type: array
    items:
      oneOf:
        - $ref: './AppCredentialBinding.yaml'
        - $ref: './KeyCredentialBinding.yaml'
      discriminator:
        propertyName: type
        mapping:
          app: './AppCredentialBinding.yaml'
          key: './KeyCredentialBinding.yaml'
    description: List of service credential bindings
  included:
    $ref: './IncludedResources.yaml'
    description: Additional related resources included in the response when using the `include` parameter
description: A paginated list of service credential bindings
