type: object
properties:
  pagination:
    allOf:
      - $ref: './Pagination.yaml'
      - description: Pagination information for the list of feature flags
  resources:
    type: array
    description: A list of feature flags
    items:
      $ref: './FeatureFlag.yaml'
description: >
  A paginated list of feature flags.
