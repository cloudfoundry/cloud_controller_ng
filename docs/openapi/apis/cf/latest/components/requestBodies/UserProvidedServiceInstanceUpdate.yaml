description: Optional parameters for updating a user-provided service instance
required: true
content:
  application/json:
    schema:
      $ref: '../schemas/UserProvidedServiceInstanceUpdate.yaml'
    examples:
      user_provided_service_instance:
        summary: Update user-provided service instance
        value:
          name: my_service_instance
          credentials:
            foo: bar
            baz: qux
          tags:
            - foo
            - bar
            - baz
          syslog_drain_url: https://syslog.com/drain
          route_service_url: https://route.com/service
          metadata:
            annotations:
              foo: bar
            labels:
              baz: qux
