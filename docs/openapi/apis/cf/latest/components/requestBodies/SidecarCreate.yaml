description: The sidecar to create
required: true
content:
  application/json:
    schema:
      $ref: ../schemas/SidecarCreate.yaml
    examples:
      default:
        summary: default
        value:
          name: auth-sidecar
          command: bundle exec rackup
          process_types:
            - web
            - worker
          memory_in_mb: 300
