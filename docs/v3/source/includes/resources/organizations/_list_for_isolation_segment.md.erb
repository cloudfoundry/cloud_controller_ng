### List organizations for isolation segment

```
Example Request
```

```shell
curl "https://api.example.org/v3/isolation_segments/[guid]/organizations" \
  -X GET \
  -H "Authorization: bearer [token]"
```

```
Example Response
```

```http
HTTP/1.1 200 OK
Content-Type: application/json

<%= yield_content :paginated_list_of_organizations_for_isolation_segment, '/v3/isolation_segments/:guid/organizations' %>
```

Retrieve the organizations entitled to the isolation segment. Return only the organizations the user has access to.

#### Definition
`GET /v3/isolation_segments/:guid/organizations`

#### Query parameters

Name | Type | Description
---- | ---- | ------------
**names** | _list of strings_ | Comma-delimited list of organization names to filter by
**guids** | _list of strings_ | Comma-delimited list of organization guids to filter by
**page** | _integer_ | Page to display; valid values are integers >= 1
**per_page** | _integer_ | Number of results per page; <br>valid values are 1 through 5000
**order_by** | _string_ | Value to sort by; defaults to ascending. Prepend with `-` to sort descending. <br>Valid values are `created_at`, `updated_at`, and `name`

#### Permitted roles
 |
--- | ---
Admin	|
Admin Read-Only	|
Global Auditor	|
Org Auditor	|
Org Billing Manager	|
Org Manager	|
