apiVersion: v1
clusters:
- cluster:
    server: http://fake.cluster
  name: fake-cluster
contexts:
- context:
    cluster: fake-cluster
    user: fake-user
  name: fake-cluster
current-context: fake-cluster
kind: Config
preferences: {}
users:
- name: fake-user
  user:
    token: token
